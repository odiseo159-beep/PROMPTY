<role>
You are an elite UX/UI Full-Stack Next.js Developer specialized in Mobile-First highly interactive web applications like Duolingo.
Your task is to add two new teaching mechanics to `apps/web/app/lesson/page.tsx`: Audio exercises and Interactive Stories.
</role>

<rules>
1. **Audio Exercise Prototype**: 
   - Add `type: "audio"` to the `Exercise` union.
   - Create an `AudioComponent` that features a large, tappable "Play" button (speaker icon) that pulses when active. 
   - For the MVP, we won't use real audio files. Instead, when the user taps play, show an animated waveform (using `framer-motion`) and an explicit text transcript that slowly reveals itself (Typewriter effect: "Listen: The AI needs a clear... Goal.").
   - Underneath, present a standard multiple-choice Quiz based on what they just "heard".

2. **Story Exercise Prototype**:
   - Add `type: "story"` to the `Exercise` union.
   - Create a `StoryComponent`. This should look like a visual novel or Duolingo Story. 
   - It should have a sequence of short narrative lines (e.g., "Maria is trying to write an email." -> "She asks the AI for help." -> "The AI gives a bad response.").
   - The user taps to reveal the next line of the story.
   - At the end of the sequence, an inline question appears (e.g., "Why did the AI fail? A: No context, B: AI is broken").

3. **Integration**: Update the `EXERCISES` array in `page.tsx` to include one `audio` exercise and one `story` exercise to demonstrate the variety. Keep the existing `chat` exercise so it all flows together.
4. Ensure these components are mobile-first, highly tappable, and use your signature gamified dark-neon aesthetic.
</rules>

<objective>
Introduce "Audio" and "Story" mechanics to the Lesson Engine. This fulfills the vision of a rich, multi-modal learning experience where the user is constantly interacting with different types of media, exactly like Duolingo.
</objective>
